package dataAccessLayer;

import Model.Client;
/**
 * The ClientDAO class extends the AbstractDAO class and provides data access methods specific to the Client class.
 * It inherits basic (Create, Read, Update, Delete) methods from the superclass.
 */
public class ClientDAO extends AbstractDAO<Client>  {
    // uses basic CRUD methods from superclass

    // TODO: create only client specific queries
}
