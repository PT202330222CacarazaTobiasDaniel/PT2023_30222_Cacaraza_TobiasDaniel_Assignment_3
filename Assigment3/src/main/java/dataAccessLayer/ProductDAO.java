package dataAccessLayer;

import Model.Product;

/**
 * The ProductDAO class extends the AbstractDAO class and provides data access methods specific to the Product class.
 * It inherits basic (Create, Read, Update, Delete) methods from the superclass
 */
public class ProductDAO extends AbstractDAO<Product> {
    // uses basic CRUD methods from superclass

    // TODO : create only product specific queries
}
